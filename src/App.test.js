import {render, screen} from '@testing-library/react';
import App from './App';

test('renders  react <App/>', () => {
    render(<App/>);
    expect(true).toBe(true);
});
test('renders  react <App/>', () => {
    render(<App/>);
    expect(true).toBe(true);
});
